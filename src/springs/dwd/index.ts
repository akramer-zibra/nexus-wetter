interface Forecast {
    // TODO
}

/** Gives an aggregat for forecast */
export const forecast = (place: string) => {

    // retrieve relevant station
    
    // retrieve relevant weather forcast for station(s)

    // aggregate data
}